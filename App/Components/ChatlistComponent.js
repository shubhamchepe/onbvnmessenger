import React from 'react';
import { Text, View } from 'react-native';

const ChatlistComponent = ({params}) => (
    <View>
        <Text>ChatlistComponent</Text>
    </View>
);

export default ChatlistComponent;
